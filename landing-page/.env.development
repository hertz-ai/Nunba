# Build configuration — tracked in git (no secrets here)
# React CRA loads this automatically in development and build modes

# Downgrade ESLint errors to warnings so the build doesn't fail
# on import order and prettier formatting issues
ESLINT_NO_DEV_ERRORS=true
DISABLE_ESLINT_PLUGIN=true
